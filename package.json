{
  "name": "cf-ai-visual-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive visual demo of Cloudflare AI use cases",
  "scripts": {
    "dev": "wrangler dev",
    "configure": "node scripts/configure.js",
    "deploy": "node scripts/configure.js && wrangler deploy",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
